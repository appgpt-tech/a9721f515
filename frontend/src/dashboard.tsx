import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "music inventory" />
      <CardContent> An application to manage the inventory of a music store < /CardContent>
      </Card>
    );